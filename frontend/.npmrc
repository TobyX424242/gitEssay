# The Lexical playground pulls @atlaskit/pragmatic-drag-and-drop (used by
# DraggableBlockPlugin) whose transitive peer pin react@^16 is stale — it works
# fine on React 19. Accept the outdated peer range instead of forking the plugin.
legacy-peer-deps=true
